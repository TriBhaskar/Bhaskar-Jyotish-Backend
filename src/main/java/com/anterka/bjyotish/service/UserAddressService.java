package com.anterka.bjyotish.service;

import lombok.AllArgsConstructor;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class UserAddressService {
    // This service is currently empty, but can be expanded in the future
    // to handle user address-related operations such as adding, updating,
    // or deleting addresses associated with a user.

    // For now, it serves as a placeholder for future functionality.

    // Example methods could include:
    // - addAddress(UserAddressRequest request)
    // - updateAddress(Long addressId, UserAddressRequest request)
    // - deleteAddress(Long addressId)
    // - getUserAddresses(Long userId)


}
